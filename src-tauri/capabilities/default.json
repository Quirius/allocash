{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local main window; no filesystem, shell or network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
